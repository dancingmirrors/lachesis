lachesis
========
This is a standalone fork of ffplay with a modified version of the build system from mpv's bliss branch.

Dependencies include FFmpeg 7.1 or greater, SDL3, SDL_ttf, and libarchive.
This is based on the current Debian stable.

Vulkan (development headers and libraries), libplacebo, and yt-dlp are optional
but recommended.
